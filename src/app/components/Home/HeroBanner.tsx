/*This is to display the hero banner under navigation bar on Home Page*/
import Image from "next/image";

export default function HeroBanner(){
    return (
        <div>
            <Image src = "" width = {1440} height = {500} alt = "hero-banner"/>
        </div>
    )
}